# Context

## Project Info
- **Project**: Fermion Roastery (Next.js Application)
- **Path**: `/home/jrilym/Projects/Next/fermion-roastery`
- **Integrity Mode**: Development

## Target Files
1. `frontend/app/account/page.tsx`
2. `frontend/app/cart/page.tsx`
3. `frontend/components/cart-sheet.tsx`
4. `frontend/app/b2b/register/page.tsx`
5. `frontend/app/b2b/contract/page.tsx`
6. `frontend/app/b2b/shop/page.tsx`
7. `frontend/app/b2b/checkout/page.tsx`
8. `frontend/app/subscription/checkout/page.tsx`

## Core Translation File
- `frontend/lib/i18n.ts` (contains `translations` object for `en` and `id`, and exports `useI18n` hook).

## Critical Guidelines
1. **User Global Rule: Copywriting Constraints**:
   - Avoid the term **"Ritual"** when referring to orders, subscriptions, or business processes (especially in B2B/Admin contexts).
   - Use straightforward business terms:
     - Indonesian: `Pesanan` (Orders), `Kemitraan` (Partnership), or `Berlangganan` (Subscription).
2. **User Global Rule: Design Preferences**:
   - Do not use decorative, expressive, or "permanent marker" style fonts (e.g. `font-cloude`) on serious/functional pages.
   - Prioritize clean, professional, readable sans-serif typography.
